import { createTheme } from '@mui/material'

//use MUI's default theme
const theme = createTheme({
    typography: {
        fontFamily: 'verdana, arial, helvetica, sans-serif', // Specify your custom font
    }
});

export default theme;
